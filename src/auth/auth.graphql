type AuthPayload {
    access_token: String!
}

input AuthInput{
    email: String!
    password: String!
}

type Mutation {
    signUp(data:AuthInput!): User!
    signIn(data:AuthInput!): AuthPayload
}
